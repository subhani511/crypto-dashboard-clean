# Example environment variables for Crypto Dashboard
# Copy this file to `.env.local` and fill in real values (never commit `.env.local`)

# Base URL for CoinGecko API (public, no key required)
COINGECKO_BASE=https://api.coingecko.com/api/v3

# If you integrate with other providers (e.g., Binance, CoinMarketCap), put keys here
# API_KEY=your_api_key_here

# Optional: set Node environment (development | production)
# NODE_ENV=development
